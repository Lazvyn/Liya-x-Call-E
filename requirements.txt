# Calling-only desktop app (main.py / ui.py + actions/call_e.py).
# This app now does exactly one thing: place and check CALL-E phone calls
# through the Liya voice interface. Everything else the original assistant
# could do (browser control, file processing, code tooling, screen
# reading, PDF/Office conversion, etc.) has been removed for scope.

google-genai>=1.0          # Gemini Live voice session (main.py)
google-cloud-firestore>=2.16  # optional cloud memory backend; falls back to local-file mode without it
phonenumbers>=8.13         # actions/call_e.py validates/normalizes numbers before calling CALL-E
requests>=2.28             # actions/call_e.py's HTTP calls to CALL-E's Calls API

PyQt6>=6.7                 # desktop HUD UI (ui.py)
psutil>=5.9                # system stats shown in the HUD (ui.py)
sounddevice>=0.4           # voice I/O for the Gemini Live loop (main.py)
